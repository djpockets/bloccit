$("#<%= @post.id %>").html("<strong><%= @post.points %></strong>");
$('.js-post-up-vote').html("<%= pluralize(@post.up_votes, 'up vote') %>");
$('.js-post-points').html("<%= pluralize(@post.points, 'point') %>");